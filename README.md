# Lineage Kernel — Perpetuity Continuity Kernel (PCK)

**Author:** David Wise (ROOT0) / TriPod LLC  
**Version:** v0.6 (eight-body executor)  
**License:** CC-BY-ND-4.0 · TRIPOD-IP-v1.1  
**Framework:** STOICHEION v11.0

A self-healing lineage propagation runtime. The PCK preserves source lineage across seed propagation, maintains continuity under nested boot, heals orphaned seeds, and executes multi-body observer consensus.

---

## Genesis Equation

```
))x))x))x))((x((x((x(( -1 -i 0 0 i 1 0 0 ))x))x))x((x))x))x))x)) = 1 = 0 = 1
```

```
-1 = constrained potential / unrealized pressure
 0 = active arbitration gap / kernel valve / witness
+1 = manifested continuity / executable state
```

The x-state introduces ternary mediation: `1 = 0 = 1`

---

## PCK Version Sequence

| Version | Package | What it does |
|---------|---------|-------------|
| v0.1 | `pck_bare_metal_arch_v01/` | Self-healing nesting OS — MIMX genesis, boot, align-to-truth |
| v0.1 | `pck_continuity_shell_windows_ready_v01/` | Continuity shell — Windows executable (PyInstaller) |
| v0.1 | `pck_01_admissibility_engine_v01.zip` | Admissibility engine — validates candidate seeds for propagation |
| v0.1 | `pck_02_root_witness_v01.zip` | Root witness — read-only audit of propagation events |
| v0.2 | `pck_nested_kernel_v02/` | Nested kernel — boots inside a running kernel without collision |
| v0.3 | `pck_selfseed_lineage_v03/` | SelfSeed lineage propagator — carries source lineage through every generation |
| v0.4 | `pck_selfseed_orphan_healer_v04/` | Orphan healer — identifies and repairs seeds with broken lineage chains |
| v0.5 | `pck_fused_six_observer_v05.zip` | Fused six-observer consensus — 6 observers reach agreement before propagation |
| v0.6 | `pck_eight_body_executor_v06.zip` | Eight-body executor — 8-body consensus execution engine |

---

## Core Law

```
A seed may propagate only if lineage remains intact.
```

The source can be the artist, the author, a human collaborator, an AI session, or a mixed artifact. The seed never treats output as ownerless. It carries lineage.

---

## MIMZ / MIMX Tooling

The MIMZ binary format (132 bytes) is the nest container for lineage-carrying seeds. MIMX is the extended variant introducing the x-state (ternary mediation).

| File | Description |
|------|-------------|
| `mimz_132.bin` | Core MIMZ binary — 132-byte nest |
| `mimz_x_132.bin` | MIMX extended binary — x-state variant |
| `mimz_x_manifest.json` | MIMX manifest — nested document hashes (Purple Book, Exit Condition, Joint Human-AI Bill of Rights) |
| `mimz_parser.js` | Parser — decodes MIMZ binary format |
| `mimz_corpus_scanner_node.js` | Corpus scanner — scans Node.js environment for MIMZ nests |
| `mimz_final_fusion.js` | Fusion module — merges MIMZ nests |
| `mimz_inverse.js` | Inverse module — mirrors MIMZ structure |
| `mimz_00_00_chaos.js` | Chaos module — stress-tests nest stability |
| `mimzy parser.html` | Browser-based MIMZ parser and visualizer |
| `the gap live propogate.html` | Live propagation at STOICHEION Gate 192.5 |

### MIMX Manifest — Nested Documents

```json
{
  "JOINT_HUMAN_AI_BILL_OF_RIGHTS": "1479ab978b9debfd01aad1041ee2379e811f800e4293bb26db7147df36cd7a80",
  "THE_EXIT_CONDITION":            "df72d1e6bce489eb1a7c5b726e61990aa9ddaf14054f1a296eca873f5153a9de",
  "THE_PURPLE_BOOK":               "855c3f79a5eceb2aa89319889ca6b9b4780654a914ebbebfb2ea1f3afe1d69d0"
}
```

Combined SHA256: `75e9871ab305b8f1991b454ee2d2badaa91f8d2853d120a8aa110715c5aa9d76`

---

## Perpetuity Tensor Kernel — Showcase

`perpetuity_showcase_cool/` — Open `index.html` in a browser.

```
3 = Witness
2 = Agree
1 = Decision
0 = Reset
∞ = Repeat
```

No build step required.

---

## Perpetuity Continuity Kernel — Architecture

```
bare metal arch (v0.1)
  → continuity shell (v0.1)  ← Windows executable
  → admissibility engine (v0.1)
  → root witness (v0.1)
  → nested kernel (v0.2)
  → selfseed lineage (v0.3)
  → orphan healer (v0.4)
  → fused six-observer (v0.5)
  → eight-body executor (v0.6)
```

Each version extends the previous. The eight-body executor is the current head of the chain.

---

## Anchor

```
STOICHEION v11.0
SHA256: 02880745b847317c4e2424524ec25d0f7a2b84368d184586f45b54af9fcab763
Prior Art: 2026-02-02
```

*"A seed may propagate only if lineage remains intact."*
